package com.company.kotlin.kotlinoop

/**
 * Destructuring Properties
 */
data class Destructuring(val id: Int, val name: String) {
    /*
    /percobaan -1, -2 --> without add "data" before initiate class Destructuring
    operator fun component1() = id
    operator fun component2() = name
     */
}